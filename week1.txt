how to link to external webiste 
alt attribute
link image to external website/ internal file
anchor tags
ul/ol
h1/h2...
<p></p>
html boilerplate - doctype / title/head/body/html tag
what is html  - standard - how it should work with
divs -group element(use classes, generates content block)
spans - group element(use classes, doesn't generate content block)
<hr/> - break line
<br/> - line break
sup/sub
html entities &;
Semantic markup - more than div 
main- non-repeated(main) content
nav - navigation
section - standalone section of content
article - heading / body - reusable content block
aside - indirectly related to main content -sidebars / callout boxes
header / footer - can be used in articles/ other content blocks
time - represents time - datetime attribute
figure - figcaption - self-contained/single unit - diagram/illustration

abbr - title - expansion/explaination

data - value =links to machine-readable translation?
emmet

Forms and tables
table -container
tr  - table row
td -cell(table data)
th - defines header
thead - format
tfoot - format 
tbody - format
table colespan and rowspan - cells that take up multiple cells






Forms

form - container
action atrtribute - where the data should be sent 
(example : /search/) http request fired(sent) to /search/ and passing it our data
groups together inputs and submits them together to a specified destination
input - accepts input from user// no closing tag
name attribute - name associated with value, that will be sent to the server
 type attribute - submit, checkbox, text, password, colour, number, month, - data types - high and low level
placeholder attribute - specifies the placeholder text
checkbox type - checked / /// name = on/off
radio = radio button /// one selection associated by the same name // value attribute - data sent when the form is submitted
select = drop down <select name="" id=""><option value=""></option> </select>
range = (volume) min / max / step /value//// (initial value)attributes  --------------same atrtributes for number type
title attribute = hint

labels - accessibility 
for attribute specifies what the label is for (id) matches all instances of the id
can also nest input elements in label elements
id attribute - specifies id
button - sends the form information to the destination specified in the form for attribute if not specified
use javascript to program the button
type attribute - specifies functionality, button, submit

textarea - id, rows, cols, name, placeholder

 -------clear / save buttons ------
 ------difference between textarea and input text------

Client side validations
required attribute
min /max - numeric
minlength - maximum length - text
pattern? - regex(p)



XML